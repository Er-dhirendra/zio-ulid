name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Test (Scala ${{ matrix.scala }}, JDK ${{ matrix.java }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        scala: ["3.3.6"]   # ← remove 2.13.16, your build.sbt is Scala 3 only
        java: ["17", "21"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup JDK ${{ matrix.java }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ matrix.java }}
          distribution: temurin
          cache: sbt

      - name: Setup sbt                      # ← ADD THIS
        uses: sbt/setup-sbt@v1              # official sbt GitHub Action

      - name: Run tests
        run: sbt ++${{ matrix.scala }} core/test

      - name: Check formatting
        run: sbt scalafmtCheck              # ← fixed: scalafmtCheck not "check"